{
  "firestore": {
    "rules": "emulator.rules"
  },
  "emulators": {
    "//": "Local only. `npm run emulator` starts these so tools/rules-audit.mjs can run firestore.rules and storage.rules through the real engine. Auth is here because the student rules turn on WHO is signed in, and a rules test with no identities can only ever test the stranger.",
    "auth": {
      "port": 9099
    },
    "firestore": {
      "port": 8080
    },
    "storage": {
      "port": 9199
    },
    "ui": {
      "enabled": true,
      "port": 4000
    },
    "singleProjectMode": true
  }
}
